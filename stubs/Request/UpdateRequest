<?php

declare(strict_types=1);

namespace App\Http\Requests\{{controllerName}};

use App\Http\Requests\FormRequest;

final class Update{{controllerName}}Request extends FormRequest
{
    /**
     * Get the validation rules that apply to the request.
     */
    protected function rules(): array
    {
        return [
            // Define your fields here
            // Example: 'name' => 'sometimes|string|max:255',
        ];
    }

    /**
     * Get custom validation messages.
     */
    protected function messages(): array
    {
        return [
            // Define custom messages here
            // Example: 'email.email' => 'Please provide a valid email address.',
        ];
    }
}
